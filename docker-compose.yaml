services:
  app:
    image: node:24-alpine
    container_name: bycode_labs
    ports:
      - ${PORT}:5173
    working_dir: /app
    volumes:
      - ./:/app
    env_file: .env
    command: sh -c "yarn && yarn dev"